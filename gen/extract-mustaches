#!/bin/zsh
ack '{{([^!}]*)}}' ${1-gen/home.html.mustache} --output='$1' | perl -ne '
	if(/^\//) { # Closing tag
		--$i;
		$x = 1;
	} else {
		print "\n" if $x;
		$x = 0;

		print "\t"x$i.$_;
		$i += /^#/; # Opening tag
		# $i += /^[#^]/; # Opening/"else" tag
	}
'
